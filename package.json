{
  "name": "cadence-events-service",
  "version": "1.0.0",
  "description": "Dagu-based service for scheduled gRPC calls",
  "main": "scripts/grpc-client.js",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
